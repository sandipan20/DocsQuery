# DocsQuery Evaluation Corpus

This directory contains the version-controlled source documents
used by the DocsQuery retrieval and end-to-end evaluation suites.

## Current benchmark corpus

- `git_cheat_sheet.pdf`
- `progit.pdf`

These documents are intentionally tracked in Git so that CI
evaluation runs against the same source corpus as local evaluation.

The corpus fingerprint is generated by:

```bash
python -m scripts.build_index


This makes the design explicit for anyone who clones the repository later.

---

# 11.14.4 — Commit the corpus files

First inspect their exact sizes:

```bash
ls -lh data/raw/*.pdf